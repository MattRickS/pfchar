World's simplest and laziest pathfinder combat helper.

Currently hardcoded character with a limited set of functionality.

### Installation

Always recommended to use a virtual environment

```
pip install -r requirements.in
```

### Launch UX
```bash
python -m pfchar.web
```
